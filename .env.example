# 複製這個檔案為 .env，並填入你自己的 Gemini API Key
# 取得金鑰（免費）：https://aistudio.google.com/apikey

GEMINI_API_KEY=your_gemini_api_key_here

# 可選，預設為 gemini-flash-latest（Google 官方別名，自動指向目前建議的 flash 模型，速度快、免費額度較高）
# 其他可用別名例如 gemini-pro-latest（能力較強、速度較慢）
# 注意：部分固定版本號的舊模型（如 gemini-2.5-flash-lite）已對新申請的帳號關閉，
# 若改用固定版本號出現 404 "no longer available to new users"，請改用 -latest 別名或 gemini-3.x 系列
GEMINI_MODEL=gemini-flash-latest

# 可選，Day 3 LangGraph 工作流（有多輪工具呼叫）專用的模型，預設跟上面的 GEMINI_MODEL 相同。
# 已知限制：Gemini 3.x 等「思考型」模型在多輪工具呼叫時要求回傳 thought_signature，
# 但目前 LangChain 的 OpenAI 相容層還不支援保留這個欄位，第二輪工具結果送回時會出現
# 400 INVALID_ARGUMENT 錯誤。如果你的帳號還能存取 gemini-2.0-flash 這類舊一代模型
# （沒有 thought_signature 限制），可以取消下面這行的註解來繞過此問題；
# 但新帳號常見對舊模型的免費配額是 0，若遇到 429 quota=0，請維持註解、使用預設模型。
# GEMINI_WORKFLOW_MODEL=gemini-2.0-flash
